// As defined in the JSON schema
export const SHORT_DESCRIPTION_MAX_LENGTH = 100;
export interface Display {
  image?: string;
  accentColor?: string;
  shortDescription?: string;
  description?: string;
}
